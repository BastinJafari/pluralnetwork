{
  "name": "plural-network",
  "private": true,
  "workspaces": {
    "packages": [
      "layers/*"
    ],
    "nohoist": [
      "**/react",
      "**/react-dom"
    ]
  },
  "nohoist": [
    "**/react",
    "**/react-dom",
    "babel-loader"
  ],
  "scripts": {
    "reset": "find . -type dir -name node_modules | xargs rm -rf && rm -rf yarn.lock"
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {}
}
